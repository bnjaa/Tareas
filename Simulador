#include <iostream>
using namespace std;

int esperanza_H = 80;
int esperanza_M = 85;

int contar_meses(int fecha_i, int edad) {
	int c = (12 - fecha_i + 1) + (65 - edad);
	return c;
}
int Jubilar(char sexo, int mes_actual, int edad, int fondos, int sueldo, int cotizacion, int voluntario) {
	
	return J;
}
int main () {
	cout << contar_meses(3, 22) << endl;
	return 0;
}
